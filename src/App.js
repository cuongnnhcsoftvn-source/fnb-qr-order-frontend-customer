import React from 'react';

export default function App() {
  return (
    <div style={{ padding: 20 }}>
      <h2>FNB QR Menu</h2>
      <p>Use / (root) for QR landing, /menu?table=ID to open menu directly.</p>
    </div>
  );
}
